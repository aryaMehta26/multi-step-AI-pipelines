# Optional: when the RocketRide engine is running locally (default extension URL).
# The Next app calls GET {ROCKETRIDE_BASE_URL}/health to show connection status on /app.
ROCKETRIDE_BASE_URL=http://127.0.0.1:5565
