# Screenshot checklist (RocketRide internship packet)

Add these images to **`docs/screenshots/`** before you email “everything is ready.”

1. **`01-canvas.png`** — `goal-planning-agent.pipe` open in RocketRide with the expanded release-readiness graph visible (agents, tools, database/context, memory, scoring, response, handoff).
2. **`01-canvas-ui.png`** — browser on `http://127.0.0.1:3000/app` showing the expanded pipeline canvas rendered in the app.
3. **`02-run-trace.png`** — same pipeline **during or after Play**, showing traces / outputs panel.
4. **`03-next-app.png`** — browser on `http://127.0.0.1:3000/app` with a completed run (node steps + markdown readiness report).

**Tip:** Hide personal API keys before capturing; blur if needed.

After exporting, reference them briefly in the root README under “RocketRide submission”.
